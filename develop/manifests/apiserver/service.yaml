apiVersion: v1
kind: Service
metadata:
  labels:
    app: beast-apiserver
  name: beast-apiserver
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: beast-apiserver
  type: ClusterIP
